apiVersion: 1
datasources:
  - name: Elasticsearch
    type: elasticsearch
    access: proxy
    url: http://elasticsearch:9200
    database: "[soc-]YYYY.MM.DD"
    basicAuth: true
    basicAuthUser: elastic
    basicAuthPassword: changeme
    isDefault: false
    editable: false
